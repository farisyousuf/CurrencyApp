package com.faris.currency.config

object Configuration {
    const val BASE_URL = "http://data.fixer.io/api/"
    const val CONNECTION_TIMEOUT: Long = 60
    const val READ_TIMEOUT: Long = 60
    const val CALL_TIMEOUT: Long = 60
}